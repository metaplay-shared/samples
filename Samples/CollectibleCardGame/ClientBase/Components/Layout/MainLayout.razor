@inherits LayoutComponentBase

<main class="container mx-auto px-4 py-5 max-w-2xl">
    @Body
</main>

@* The connection shell sits above every screen, so a connection that has stopped working takes input away from
   the whole app and no screen checks connection state for itself (see ConnectionTroublePolicy). *@
<ConnectionShell />
