# Configuration files to use
config:
  files:
    - "./Config/Options.base.yaml"
    - "./Config/Options.dev.yaml"
    - "./Config/Options.presently.yaml"

publicWebApi:
  enabled: true

experimental:
  gameserversV0Api:
    enabled: true
